#!/bin/sh
# Runs before every git push. Blocks the push if types, tests, or lint fail.
set -e

echo "Running type check..."
./node_modules/.bin/tsc --noEmit

echo "Running tests..."
npx vitest run

echo "Running lint..."
npm run lint

echo "All checks passed."
